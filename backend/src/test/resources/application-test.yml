spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://localhost:5432/opsdesk_test}
    username: ${TEST_DB_USERNAME}
    password: ${TEST_DB_PASSWORD}

opsdesk:
  jwt:
    secret: test-only-jwt-secret-that-is-longer-than-32-bytes
    expiration-seconds: 3600
  ai:
    url: http://127.0.0.1:1
    timeout-milliseconds: 100
